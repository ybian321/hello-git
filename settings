# repo

/.git/config

# user computer

~/.gitconfig

# computer system

$(prefix)/etc/gitconfig

git config --global color.ui true
git config --local user.email example@gmail.com
git config --global alias.st status

Three tree Architecture: working, staging, reposotiry
